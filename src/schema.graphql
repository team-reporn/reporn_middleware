type Query {
  info: String!
  users: [User!]!
}

type Mutation {
  post(email: String!, login: String!, password: String!): User!
}

type User {
  id: ID!
  email: String!
  username: String!
  login: String!
}
